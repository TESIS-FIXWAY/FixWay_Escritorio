import { describe, it, expect } from 'vitest'
import Mecanico from '../components/mecanico/mecanico'

describe('Mecanico', () => {
  it('should be a function', () => {
    expect(typeof Mecanico).toBe('function')
  })
})