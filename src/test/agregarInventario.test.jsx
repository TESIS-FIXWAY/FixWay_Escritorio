import { describe, it, expect } from 'vitest'
import AgregarInventario from '../components/admin/agregarInventario'

describe('AgregarInventario', () => {
  it('should be a function', () => {
    expect(typeof AgregarInventario).toBe('function')
  })
})