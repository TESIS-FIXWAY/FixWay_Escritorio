import { describe, it, expect } from 'vitest'
import GestionMantencionAdmin from '../components/admin/gestionMantencionAdmin'

describe('GestionMantencionAdmin', () => {
  it('should be a function', () => {
    expect(typeof GestionMantencionAdmin).toBe('function')
  })
})