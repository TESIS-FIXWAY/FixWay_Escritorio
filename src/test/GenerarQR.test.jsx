import { describe, it, expect } from 'vitest'
import App from '../components/mecanico/GenerarQR'

describe('App', () => {
  it('should be a function', () => {
    expect(typeof App).toBe('function')
  })
})