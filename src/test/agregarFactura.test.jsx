import { describe, it, expect } from 'vitest'
import AgregarFactura from '../components/admin/agregarFactura'

describe('AgregarFactura', () => {
  it('should be a function', () => {
    expect(typeof AgregarFactura).toBe('function')
  })
})