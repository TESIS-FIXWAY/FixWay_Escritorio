import { describe, it, expect } from 'vitest'
import ListarUsuario from '../components/admin/listarUsuario'

describe('ListarUsuario', () => {
  it('should be a function', () => {
    expect(typeof ListarUsuario).toBe('function')
  })
})