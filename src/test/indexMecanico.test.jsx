import { describe, it, expect } from 'vitest'
import IndexMecanico from '../components/mecanico/indexMecanico'

describe('IndexMecanico', () => {
  it('should be a function', () => {
    expect(typeof IndexMecanico).toBe('function')
  })
})